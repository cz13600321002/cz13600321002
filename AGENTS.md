# AGENTS.md

> 面向 AI 编码/工作代理（如 Codex、GPT-6 Astra、Cursor Agent 等）的仓库指南。
> 人类使用者请优先阅读 `README.md`；本文件用于规范代理行为，避免臆造与越权操作。

## 关于本仓库

本仓库是「工作文库」——一个**纯文档知识库**，用于存放工作文档模板、项目资料与归档内容。
仓库中**没有代码、没有构建/测试系统、没有依赖安装步骤**。所有变更都是对 Markdown 文档的增删改。

## 指令优先级（Instruction Priority）

当出现冲突时，按以下顺序取更高优先级的规则，不要混用低优先级规则覆盖高优先级规则：

1. **当前会话中用户的直接指令**（本次任务的明确要求，优先级最高）
2. **`AGENTS.override.md`**（如存在于仓库根目录或相关子目录）——用于临时性、一次性的规则覆盖，仅在其存在期间生效
3. **子目录下更贴近的 `AGENTS.md`**（如存在）——仅在该子目录范围内覆盖本文件对应条款
4. **本文件**（仓库根目录 `AGENTS.md`）
5. **`README.md`**——面向人类的背景说明与目录结构文档，供参考，不构成强制规则；若与本文件冲突，以本文件为准

若指令仍不明确：优先保守处理（不写入敏感信息、不臆造未验证的事实），并在提交说明或 PR 描述中注明所做的假设。

## 仓库结构（简要，详见 README.md）

- `模板/` —— 六类文档模板：会议纪要、周报、月报、工作总结、工作方案、项目计划（当前唯一已创建的目录）
- `文档/`、`项目/`、`资料/`、`归档/` —— README 中规划的目录，若尚未创建，创建前先确认确有需要，不要为了"补齐结构"而批量新建空目录

## 构建 / 测试 / 检查

本仓库无代码，因此没有传统意义上的 build/test/lint 命令。提交前请手动核对：

- 新文档是否放入了正确的目录
- 文件名是否符合下方命名规范
- 模板占位符（如空表格单元格、`（说明…）`提示文字）是否已被实际内容替换，且未遗留未填写的模板骨架
- 若需要格式化校验，可选执行 `markdownlint '**/*.md'`（本仓库未强制要求安装）

## 约定（Conventions）

- **文件命名**：`YYYYMMDD_文件名称.扩展名`
- **项目文件夹命名**：`项目名称_YYYY`
- **归档文件夹命名**：`YYYY年/月份/`
- **新建文档**：从 `模板/` 对应子目录复制模板，填写后保存到 `文档/` 对应子目录（模板本身不要直接修改内容，仅在明确要求"更新模板结构"时才编辑模板文件）
- **提交信息（commit message）**：使用中文，简明描述本次改动内容，与现有历史提交风格一致（例如「建立工作文库基础结构」）
- 保持与仓库现有文档一致的 Markdown 风格：`#`/`##` 标题分级、`---` 分隔线、表格用于结构化信息

## 禁止事项（Do-Nots）

- **不得**在任何文档、模板、示例或提交记录中写入真实的工厂/生产机密、内部配方代号（如 C0 等）、客户名单，或未经核实的食品接触（食品级）合规声明
- **不得**臆造仓库中不存在的事实、数据、历史记录或人名；无法确认时应标注「待确认」，不要编造具体数值
- **不得**覆盖或删除 `归档/` 目录下已归档文件的原始内容
- **不得**为本仓库引入代码、构建工具链或与"文档知识库"定位无关的内容
- **不得**在没有明确指示的情况下批量创建空的规划目录（`文档/`、`项目/`、`资料/`）

## 完成定义（Definition of Done）

一次变更被视为完成，需满足：

- 新增/修改文档位于正确目录，文件命名符合规范
- 模板占位内容已替换为实际内容或明确删除，无残留的空模板骨架
- 未包含上方「禁止事项」中列出的任何敏感信息
- 变更已使用清晰的中文提交信息 `git commit`，并已 `git push`
- 如任务要求提交 PR，已创建/更新 PR，且 PR 描述中说明了改动范围与关键假设

## 临时覆盖（Overrides）

如某次任务需要临时打破以上规则（例如一次性允许创建规划目录骨架），应在仓库根目录新建
`AGENTS.override.md` 并说明覆盖范围与失效条件；任务完成、覆盖不再需要时应删除该文件，
避免其变成长期存在的、与本文件矛盾的规则。当前仓库中不存在该文件。
